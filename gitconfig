[sendemail]
	smtpserver = smtp-mail.outlook.com
	smtpserverport = 25
	smtpencryption = tls
[user]
	email = cslilingfei@outlook.com
	name = lingfei
[color]
    # Enables color for git commands like status, diff, etc.
    ui = auto
[core]
    # Makes color work with output longer than one screen.
    pager = less -FMRiX 
    # Tells Git to ignore files generated by the build system
    excludesfile = /home/lingfl/.config/git/excludes
[push]
    # This is much safer than the Git-1.x default, and will be the default for Git-2.0.
    default = simple
[alias]
    # A convenient way to view the DAG of commits. Type 'git dag' to use it.
    dag = log --graph --format='format:%C(yellow)%h%C(reset) %C(blue)\"%an\" <%ae>%C(reset) %C(magenta)%ar%C(reset)%C(auto)%d%C(reset)%n%s' --date-order
